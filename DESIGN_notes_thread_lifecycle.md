# 后处理线程生命周期：v31（常驻+退避） vs 完全停线程版

## 结论

完全停线程版在账面上严格更省，但省下来的量级已经贴近噪声底；代价是一次真实的鲁棒性降级，外加一段生命周期竞态。除非要求“绝对零参与”的洁癖，否则不划算。若要进一步省，优先拉长 `IDLE_POLL_MS`，而不是停线程。

---

## 一、空闲态开销

**v31**：线程常驻（daemon）。空闲时每 3 秒醒一次，动作是读 `mWideActive`、比较 `mWideLastSeenAt`、调用一次 `isWideSelected(sLp)`。最后一项是反射进相机自身的 DataManager 读比例偏好，同进程、不跨 Binder，但并非零成本——存在对象分配可能，也可能碰到对方的锁。量级：每次几十微秒，3 秒一次，平均 CPU 占用约 0.001%–0.01%。

**停线程版**：空闲判定成立时线程 `run()` 直接 return，内核调度队列中不再有这个任务，连定时器都不挂载。开销严格为零。

---

## 二、唤醒路径（关键差异）

**v31** 有两条路径把线程拉回全速：

1. `hookRatioGetter` 检测到 wide → `wakePostProcessor()` → `notifyAll()`，即时。
2. 线程 3 秒后自行醒来，重新计算活跃条件。

第 2 条是**兜底的自愈能力**。若 wide 信号在 hook 层未被捕获（相机走了不经过 `u7.q0.b` 的路径，或 `mRatioDataKey` 那一轮未解析出来），线程最多 3 秒后自行发现相机仍在宽幅，切回 700 ms。

**停线程版** 只有一条路径。线程已死，唯一能复活它的是 hook 回调里的启动逻辑。一旦 wide 信号在 hook 层漏掉，线程永不被拉起，兜底彻底哑火——不是延迟 3 秒，而是根本不发生。

而兜底存在的全部意义，恰恰是接住主路径漏掉的那些帧。为省 0.01% CPU 而把安全网的触发条件收窄为“主路径必须成功”，逻辑上自相矛盾。

---

## 三、生命周期竞态

**停线程版** 必须处理一个窗口：

```
线程判定应退出 → 尚未真正退出
        ↑
   此时 wide 信号到达
        ↓
唤醒逻辑看到线程“仍存活” → 不启动新线程
        ↓
线程退出 → 线程丢失
```

修法：把“标记死亡”与“启动检查”放入同一把锁，或线程退出前再计算一次活跃条件。代价约 15–25 行，新增一处可能出错的地方。

**v31** 完全没有该问题。线程常驻，只有一对 `wait/notify`，无启停、无存活判定。

---

## 四、优劣对照

| 维度 | v31 | 完全停线程 |
|---|---|---|
| 空闲 CPU | 每 3 s 一次微秒级唤醒 | 严格为零 |
| 兜底自愈 | 有，最多 3 s 延迟 | 无，仅靠 hook 单点触发 |
| 竞态风险 | 无 | 启停窗口需额外加锁 |
| 代码量 | `sleepPostProcessor` + `wakePostProcessor`，约 20 行 | 额外 15–25 行守卫逻辑 |
| 实测差异 | — | 基本测不出 |

---

## 五、唯一可能出现体感差异的场景

相机的 DataManager 读操作存在锁竞争时，v31 每 3 秒那一次反射可能偶尔阻塞在他人锁上，造成极微小抖动。停线程版可消除该点。属理论层面，日常拍摄无体感。

---

## 六、推荐路径

1. **先拉长 `IDLE_POLL_MS`**：3000 → 10000 或 30000。开销降 3–10 倍，零新增复杂度，自愈能力不丢。性价比最高。
2. **混合式（仅当追求极致）**：空闲持续超过某阈值（如 5 分钟）后线程才真正退出，退出前保留一次活跃条件二次检查作为自愈钩子。
3. 完全停线程：不推荐作为默认方案。
